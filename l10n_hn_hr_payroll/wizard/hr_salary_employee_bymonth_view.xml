<?xml version="1.0" encoding="utf-8"?>
<odoo>
        <record id="view_salary_employee_month" model="ir.ui.view">
            <field name="name">Hr monthly Employee Salary</field>
            <field name="model">hr.salary.employee.month</field>
            <field name="arch" type="xml">
            <form string="Salary by Category">
                <span class="o_form_label">This wizard will print report which displays a payslip employees for a specified dates in reduced format.</span>
                <footer>
                    <button name="print_report" string="Print" type="object" class="btn-primary" data-hotkey="q"/>
                    <button string="Cancel" class="btn-secondary" special="cancel" data-hotkey="x" />
                </footer>
                <group>
                    <group>
                        <field name="start_date"/>
                        <field name="end_date"/>
                        <field name="category_id"/>
                    </group>
                    <group>
                        <field name="employee_company_id" options="{'no_create': True, 'no_open': True}"/>
                        <field name="employee_branch_ids" invisible="not employee_company_id" widget="many2many_tags" options="{'no_create': True, 'color_field': 'color'}"/>
                        <field name="struct_id"/>
                    </group>
                    <separator string="Employees" />
                    <field name="employee_ids"  nolabel="1" colspan="4"/>
                </group>
            </form>
            </field>
        </record>

        <record id="action_salary_employee_month" model="ir.actions.act_window">
            <field name="name">Yearly Salary by Category</field>
            <field name="res_model">hr.salary.employee.month</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>

        <menuitem
            name="Employee Salary by Category"
            parent="hr_payroll.menu_hr_payroll_report"
            sequence="200"
            action="action_salary_employee_month"
            id="menu_salary_employee_month"
            groups="hr_payroll.group_hr_payroll_manager,hr_payroll.group_hr_payroll_user"
           />
</odoo>
