<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Vista heredada de resource.calendar para agregar campos de turno -->
        <record id="view_resource_calendar_form_inherit" model="ir.ui.view">
            <field name="name">resource.calendar.form.inherit</field>
            <field name="model">resource.calendar</field>
            <field name="inherit_id" ref="resource.resource_calendar_form"/>
            <field name="arch" type="xml">
                <!-- Agregar campos después de hours_per_day -->
                <xpath expr="//field[@name='hours_per_day']" position="after">
                    <field name="nocturna"/>
                    <field name="es_turno_24_horas"/>
                    <field name="es_nomina_semanal"/>
                </xpath>
            </field>
        </record>
    </data>
</odoo>
